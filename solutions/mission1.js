// Installer nodeJS depuis https://nodejs.org/fr/download
// Puis ouvrez le terminal et exécutez la commande suivante pour vérifier l'installation:
// node -v
// Si une version de node apparait, c'est que l'installation a réussi